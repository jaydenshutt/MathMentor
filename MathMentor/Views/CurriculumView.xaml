<UserControl x:Class="MathMentor.Views.CurriculumView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:MathMentor.Converters">
    <UserControl.Resources>
        <conv:StarsConverter x:Key="Stars"/>
        <conv:BoolToVisibilityConverter x:Key="BoolVis"/>
    </UserControl.Resources>
    <Grid Margin="28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Curriculum" Style="{StaticResource PageTitle}"/>
            <TextBlock Text="Jump to any lesson anytime, free review, no gates." Style="{StaticResource PageSubtitle}"/>
            <TextBox Style="{StaticResource ModernTextBox}"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Tag="Search lessons."
                     Margin="0,0,0,12"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Margin="0,0,0,16">
            <ItemsControl ItemsSource="{Binding CategoryFilters}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Title}"
                                Style="{StaticResource SecondaryButton}"
                                Margin="0,0,8,0"
                                Padding="12,8"
                                Command="{Binding DataContext.SelectCategoryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding Id}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Groups}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,0,0,24">
                            <TextBlock Text="{Binding Title}" Style="{StaticResource SectionHeading}" FontSize="20"/>
                            <TextBlock Text="{Binding Description}" Style="{StaticResource BodyText}" Margin="0,0,0,12"/>
                            <ItemsControl ItemsSource="{Binding Lessons}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource CardBorder}" Margin="0,0,0,8" Padding="16" Cursor="Hand"
                                                MouseLeftButtonUp="Lesson_Click" Tag="{Binding LessonId}">
                                            <DockPanel>
                                                <StackPanel DockPanel.Dock="Right" VerticalAlignment="Center" Margin="16,0,0,0">
                                                    <TextBlock Text="{Binding Stars, Converter={StaticResource Stars}}"
                                                               Foreground="{DynamicResource WarningBrush}" HorizontalAlignment="Right"/>
                                                    <TextBlock Text="{Binding Score, StringFormat={}{0}%}"
                                                               Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" HorizontalAlignment="Right"/>
                                                </StackPanel>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="15"
                                                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                                                    <TextBlock Text="{Binding Subtitle}" Foreground="{DynamicResource TextSecondaryBrush}"
                                                               Margin="0,4,0,2" TextWrapping="Wrap"/>
                                                    <TextBlock Text="{Binding Meta}" Foreground="{DynamicResource AccentBrush}" FontSize="12"/>
                                                </StackPanel>
                                            </DockPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
